<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FStvol.Pages.TvolLandingPage">
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Trees" Command="{Binding ShowTreesCommand}"/>
            <Button Text="Profiles" Command="{Binding ShowProfilesCommand}"/>
            <Button Text="Reports" Command="{Binding ShowReportsCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>